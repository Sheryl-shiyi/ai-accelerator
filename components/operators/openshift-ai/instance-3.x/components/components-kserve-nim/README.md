# components-kserve-nim

## Purpose

This component enables NVIDIA NIM (NVIDIA Inference Microservices) support in KServe.

## Usage

Add this component to your overlay's `kustomization.yaml`:

```yaml
components:
  - ../../components/components-kserve-nim
```
